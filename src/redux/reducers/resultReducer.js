// init state []
const initState = {
    result: ''
}
// reducer
const resultReducer = (state = initState, action ) => {
    switch(action){
        default:
            return state
    }

}

export default resultReducer